services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-sababisha}
      POSTGRES_USER: ${POSTGRES_USER:-sababisha}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-sababisha-local}
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 5s
      retries: 5
volumes:
  postgres_data:
